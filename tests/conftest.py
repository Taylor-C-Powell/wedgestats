"""Shared fixtures and tolerance constants for wedgestats tests."""

import pytest

# Absolute tolerance for comparing floats against scipy reference values
ABS_TOL = 1e-10

# Relative tolerance for comparing floats
REL_TOL = 1e-9
